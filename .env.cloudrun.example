# Copy this file to .env.cloudrun.local and fill real values.
# The helper script reads .env.cloudrun.local and injects Cloud Run-like defaults.

# At least one transcription provider key is required
ASSEMBLYAI_API_KEY=
GEMINI_API_KEY=
GEMINI_MODEL_NAME=models/gemini-3-pro-preview

# Optional (for timestamp re-sync)
REV_AI_API_KEY=

# Auth and Secret Manager
JWT_SECRET_KEY=change-me-to-a-long-random-string
GOOGLE_CLOUD_PROJECT=
